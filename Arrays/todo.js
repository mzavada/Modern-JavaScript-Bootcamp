const todos = ['Learn JavaScript', 'Workup Local Needs', 'Perpare for Elders Meeting', 'Sleep', 'Brush teeth']

console.log(`
You have ${todos.length} todos
Todo #1: ${todos[0]}
Todo #${todos.length-1}: ${todos[todos.length-2]}
`)