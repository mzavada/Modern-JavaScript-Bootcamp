let person = {
    name: "Matthew Zavada",
    age: 38,
    location: "Tuxedo Park, NY"
}

console.log(`${person.name} is ${person.age} years old and lives in ${person.location}`)

person.age++

console.log(`${person.name} is ${person.age} years old and lives in ${person.location}`)